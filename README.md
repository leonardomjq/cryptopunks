# cryptopunks

CryptoPunks marketplace UI clone in React. Pulls a test collection from the OpenSea Rinkeby API and shows the selected punk with its owner info.

```sh
npm install
npm start
```

The OpenSea testnet API and Rinkeby are both gone, so the list comes back empty now. Kept here for reference, not maintained.
